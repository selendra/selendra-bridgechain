[package]
name = "sig-store"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# The Postgres source of truth. bridge-db reuses bridge-core's trust-boundary
# validation (id<->params binding + signature authentication) — the sig-store is
# unauthenticated, so the DB layer must verify every write.
bridge-core = { workspace = true }
bridge-db = { workspace = true }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
axum = { workspace = true }
clap = { workspace = true }
