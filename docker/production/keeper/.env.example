# ─── MACHINE 4: the keeper ────────────────────────────────────────────────────
# cp .env.example .env, fill it in, then `chmod 600 .env`.

BRIDGE_IMAGE=selendra/bridge:local
BRIDGE_PULL_POLICY=missing

# The relay-scope token, issued by whoever runs the store machine.
SIG_STORE_KEEPER_TOKEN=

RUST_LOG=info
