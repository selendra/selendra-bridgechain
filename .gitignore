.env
debridge_node/.env
secrets/*
pgdata/

.vs/
node_modules/
/debridge_node/log
dist/
data
# ...but the frontend's src/data is real source (formatters/presentation), not a
# runtime store — the bare `data` rule above would otherwise silently drop it.
!bridge/frontend/src/data/
!bridge/frontend/src/data/**
/debridge_node/keystore.json

.DS_Store
heap_info.json
config/chains_config.json

.idea/*
debridge_node/bundlr_wallet.json
.env

.cache
.local
.claude
target
sig-*
.live-*
bridge/contracts/fixtures/swap-deploy.env
